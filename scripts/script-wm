#! /usr/bin/env bash

# Setup configuration files
cp -r "$HOME/Source/distro-hopping/config/bspwm" "$HOME/.config/bspwm"
cp -r "$HOME/Source/distro-hopping/config/sxhkd" "$HOME/.config/sxhkd"
cp -r "$HOME/Source/distro-hopping/config/picom" "$HOME/.config/picom"
cp -r "$HOME/Source/distro-hopping/config/polybar" "$HOME/.config/polybar"
cp -r "$HOME/Source/distro-hopping/config/rofi" "$HOME/.config/rofi"
